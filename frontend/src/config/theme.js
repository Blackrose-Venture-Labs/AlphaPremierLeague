/**
 * Theme Configuration
 * 
 * Change the primary accent color here to update the entire application.
 * This is the single source of truth for the Bloomberg Terminal theme.
 */

export const theme = {
  // Primary accent color (Whitish Gray)
  primary: '#D1D5DB',
  primaryLight: '#E5E7EB',
  primaryDark: '#9CA3AF',
  
  // Background colors
  background: '#0f0f0f',
  surface: '#1a1a1a',
  
  // Text colors
  textPrimary: '#FFFFFF',
  textSecondary: '#9CA3AF',
  textMuted: '#6B7280',
};

export default theme;
